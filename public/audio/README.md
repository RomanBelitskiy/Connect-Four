# Background music

Place your music file here:

```
public/audio/background.mp3
```

The app loads it from `/audio/background.mp3`.

Supported formats: MP3 (recommended), or any format supported by the browser's `<audio>` element.

If the file is missing, the music toggle will still work but playback will fail silently until you add the file.
